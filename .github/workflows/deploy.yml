name: Deploy to Render
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        run: |
          curl -f "https://api.render.com/deploy/srv-d9k4u7daeets73a6jcmg?key=3xDdhBixpck"
